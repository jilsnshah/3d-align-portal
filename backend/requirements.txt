fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.13
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
itsdangerous==2.2.0
argon2-cffi==23.1.0
python-dotenv==1.0.1

# Google Maps calls (Routes + Geocoding) go out over httpx.
httpx==0.28.1

# Google Drive (optional — only needed when STORAGE_BACKEND=drive)
google-api-python-client==2.156.0
google-auth==2.37.0

# Refrens invoicing (optional — only needed when billing is configured)
pyjwt[crypto]==2.10.1
requests==2.32.3

# Dental council registry lookup (optional)
beautifulsoup4==4.12.3
rapidfuzz==3.11.0
email-validator==2.2.0

# 3D viewer: STL parsing and the A3DM conversion (meshes.py), plus the trimmed
# ICP that registers staged arches into the patient's bite (articulation.py).
# Pinned to versions with wheels for both 3.9 (local) and 3.11 (the image).
numpy==2.0.2
scipy==1.13.1

# S3-compatible object storage (Supabase Storage, R2, B2) — STORAGE_BACKEND=s3.
boto3==1.35.92

# Web push: identifies this server to the push services.
pywebpush==2.0.0
